plugins {
    id 'org.jetbrains.kotlin.jvm'
    id 'java-gradle-plugin'
    id 'maven-publish'
}

group = 'com.android.string.plugin'
version = VERSION

configurations.all {
    resolutionStrategy {
        force "com.android.tools.build:gradle:7.4.0"
        force "com.android.tools.build:gradle-api:7.4.0"
    }
}

dependencies {
    implementation gradleApi()
    implementation localGroovy()
    implementation "org.ow2.asm:asm:9.10.1"
    implementation "com.squareup:javawriter:2.5.1"
    compileOnly "com.android.tools.build:gradle:8.13.2"
    implementation project(":stringplugin-common")
}


gradlePlugin {
    plugins {
        stringblur {
            id = 'stringblur'
            implementationClass = 'com.android.string.plugin.StringBlurPlugin'
        }
    }
}

publishing {
    repositories {
//        maven {
//            url System.getenv("MAVEN_URL")
//            credentials {
//                username System.getenv("MAVEN_UPLOAD_USER")
//                password System.getenv("MAVEN_UPLOAD_PASS")
//            }
//            allowInsecureProtocol true
//        }
        maven {
            url '../../../maven/gradle'
        }
    }
}

afterEvaluate {
    publishing.publications.named('pluginMaven') {
        artifactId = 'stringblur'
    }
}